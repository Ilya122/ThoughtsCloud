function getRand(min, max) {
    return Math.random() * (max - min) + min;
}

class Weather {
    constructor() {
        this.WordsColor = "";
        this.Color = "#b4ddff";
    }

    Update(context, canvas) {

    }

    Draw(context, canvas) {

    }
}